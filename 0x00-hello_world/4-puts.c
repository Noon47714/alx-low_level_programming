#include <stdio.h>
/**
 * main - A promgram that prints a line with puts function
 * Return: 0 (Success)
 */
int mani(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}

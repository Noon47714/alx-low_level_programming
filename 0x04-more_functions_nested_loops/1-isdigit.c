#include "main.h"

/**
 * _isupper - check if a letter is upper
 * @x: The number to be cheched
 * Return: 1 for characcter that will be a digit or 0 for eny else
 */

int _isdigit(int x);

{
	if (x >= 48 && x <= 57)
	{
		return (1);
	}
	return (0);
}
